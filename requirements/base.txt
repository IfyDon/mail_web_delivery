# ── Core ──────────────────────────────────────────────────────────────
Django>=4.2.16,<5.0
djangorestframework>=3.15
djangorestframework-api-key>=3.0
django-allauth>=0.63.3
django-otp>=1.3
django-cors-headers>=4.3
django-ratelimit>=4.1
django-cookie-consent>=0.4
django-filter>=23.5
drf-spectacular>=0.27          # OpenAPI / Swagger docs
drf-spectacular[sidecar]>=0.27 # bundles Swagger UI / ReDoc assets locally
django-redis>=5.4              # Redis cache backend

# ── Database ──────────────────────────────────────────────────────────
# psycopg2-binary is dev-only — see requirements/dev.txt
# psycopg2 (compiled) is prod-only — see requirements/prod.txt

# ── Async / queue ─────────────────────────────────────────────────────
celery[redis]>=5.4             # 5.3.x Kombu incompatible with redis-py 5+
django-celery-beat>=2.5        # periodic tasks
redis>=5.0                     # no upper cap — allow security patches

# ── AWS / email relay ─────────────────────────────────────────────────
boto3>=1.34                    # AWS SES
django-anymail>=10.2           # unified ESP adapter (SendGrid, Mailgun …)

# ── DNS verification ──────────────────────────────────────────────────
dnspython>=2.6

# ── Security ──────────────────────────────────────────────────────────
cryptography>=42.0             # DKIM signing + Fernet tokens
python-jose>=3.3               # JWT (if needed)
argon2-cffi>=23.1              # Argon2id password hashing

# ── Utilities ─────────────────────────────────────────────────────────
Pillow>=12.2.0                 # tracking pixel; CVE-2023-50447 fixed >=10.2
python-dotenv>=1.0
requests>=2.31
whitenoise>=6.6                # static files in production
gunicorn>=21.2                 # WSGI server
Jinja2>=3.1                    # template rendering in template_service.py
python-json-logger>=2.0        # structured JSON log output for ELK/Datadog

# ── Billing ───────────────────────────────────────────────────────────
stripe>=7.0                    # Stripe billing SDK

# ── Geo & device tracking ───────────────────────────────────────────────────────────────────
geoip2>=4.8                    # MaxMind GeoLite2 country/city lookups
user-agents>=2.2               # parse User-Agent strings for browser/OS/device
